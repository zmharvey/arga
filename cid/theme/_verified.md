# Theme & Narrative — verification

**Status: FAIL**
Theme does not release. One systemic defect (the Tone id space was renumbered and no sibling was
updated, so ~30 cross-references in this category and four others resolve to nothing), two live
intra-category contradictions that would change a shipped string, one missing canonical term, and one
citation conflict on the evidence that justifies the category's most expensive ban.

**Scope read in full:** `_category.md`, six `_lead.md`, 20 leaf sheets. Also read for verification:
`bridge/schema.mjs` (`SCHEMA`, `playerFacingStrings`, `PROSE_PATHS`, `crossCuttingProblems`),
`docs/cid-workflow.json` (`theme-narrative-verification.checks`), `CLAUDE.md`. Build Capability
Registry taken as the `ui-forge` pattern registry — two patterns, `modal-grid` and `hud-overlay`,
per `CLAUDE.md` known gap 5 and `cid/theme/lore/_lead.md`'s own read of
`ui-forge/src/compose/patterns/`. No shell available this run, so the orchestrator's gate results
(`cid:verify` PASS, `bridge` COMPLETE 26/26, `npm test` 201/0) are taken as given and not re-run;
every finding below is from file contents.

## Check results

| # | check | result | evidence |
|---|---|---|---|
| 1 | every term this category introduces appears once in the Vocabulary canonical list, spelled one way | **FAIL** | `fantasy/03-inhabiting.md` coins `sure work` and declares `"collectedBy": "vocabulary's canonical list, marked internal"`. `vocabulary/03-term-register.md`'s `internalTerms` holds exactly two entries, `works` and `finder`, and its criterion 1 asserts "**28** internal rows over **26** manifest values plus **2**". `vocabulary/04-coinage-intake.md` caps authored entries at 2 and then asserts "six sibling sheets state *this sheet coins zero terms* … All six pass as written" — false for `fantasy/03`, which states it coins one. `grep "sure work" cid/` returns `fantasy/03`, `_digest.md`, `gameplay/meta/07` and no Vocabulary sheet. |
| 2 | no tone rule contradicts the fantasy; no lore entry asserts a world rule Setting forbids | **FAIL** (on the tone half; the lore/setting half passes) | (a) `tone/01-register.md` P8: "`old` and `ancient` are permitted". `lore/01-the-past.md` L3 forbids `ancient` and its `amends` block requests it into `vocabulary.bannedWords` with reason "L3 — dates the place". If lore's 25 merge, tone's P8 is illegal. (b) `identity/02-role-legibility.md` sets `"secondPersonPermitted": true` and rules "Second person is fine; *Clear a wider sweep as you walk* passes"; `tone/01` P1 bans second person and lists that exact string in `breaksOnMerge` with a replacement. Two sheets in one category give opposite verdicts on one shipped string. (c) `tone/01`'s `disjointFrom` claims disjointness from "`lore/01-the-past.md` L1/L3/L5"; the P8 group and lore's L3 list share `lost`, `forgotten`, `cursed`, `haunted`. **Lore/Setting half passes:** L1–L6 and S1–S6 checked against `setting.law` R1–R6, `setting.contents` P1–P12/A1–A25 and `setting.extent`; no conflict found, and `setting/03` explicitly ratifies `S4` rather than spending it. |
| 3 | every named character in Identity has a faction or an explicit "unaffiliated" | **PASS** | `identity/04-no-cast-declaration.md` enumerates nine entity classes at zero. `identity/01-player-role.md` criterion 3: "names **0** persons, peoples, factions, groups or creatures"; its one term `finder` is `"playerFacing": false`, `"unique": false`, `"office": "none"`. `identity/03` names no individual and forbids nine asymmetric fictions by name. `identity/_lead.md` reports factions and relationships empty by construction. Searched all four sheets for a proper noun denoting a being: none. Vacuously satisfied and stated, which the category brief calls the correct result. |
| 4 | nothing specifies a mechanic, an economy or pacing number; orderings and ratios stay with the owning domain | **FAIL** | `fantasy/03-inhabiting.md`'s proposed `assurances.scope` carries `"clearsPerLapAtDepth1": 140`, `"findsPerLap": 3`, `"areaCompletionsPerLap": 1`, `"lapsPerBoundSession": [3, 7]` and `"fractionOfClearsCarryingNothingAboveATick": {"atShippedValues": 0.95, "floor": 0.95}`. Every one is a pacing figure owned by `gameplay/core-loop` and `collection.relicsPerArea`, copied into a Theme key with no `source` and no re-derivation rule. If `relicsPerArea` moves off 3 the key silently ships a false figure — the `collection.total` defect class exactly. Lesser instances, noted not failed, because each is explicitly labelled as a citation of another key: `setting/01`'s `readability.derivedFrom` copies `tiers[0].rgb`; `setting/04`'s criterion 3 copies `area.size` 120 and `movement.baseWalkSpeed` 16; `identity/02` copies `baseClearRadius` 5.5; `identity/03` copies four `plots` figures immediately after writing `"doNotCopyTheNumber"`. |
| 5 | the tone sheets are specific enough to act on: a register and a humor level, not an adjective | **PASS** | `tone/01` ships nine named rules with checks: P1 person (13 banned words), P2 mood (declarative, one bare-verb exemption), P3 12 words / 2 sentences, P4 an explicit character set, P5 Flesch-Kincaid 5.0 target / 6.0 ceiling with a stated scoring corpus, P6/P7/P8 three word lists, P9 no stored all-caps. `tone/02` ships the humor level as a proportion and a mechanism: exactly one contract path, at most 6 of 24, `"mechanism": "anticlimax"` with a definition, 15 excluded mechanisms, and three name tests N1–N3. Neither sheet rests on an adjective. This is the strongest work in the category. |

## Universal invariants

- **No sheet contradicts a `[brief: binding]` item — PASS.** Every `## Pushing back` overrules a `[brief: soft]` or `[I assumed]` item only, and each names the line and its tag. `setting/02` reinterprets the binding word *deeper* as horizontal; it declares the reinterpretation, keeps every stated property of the axis, and routes the ruling upward rather than asserting it. That is the compliant mechanism.
- **2–4 acceptance criteria per leaf sheet — PASS on shape, FAIL on truth.** All 20 leaf sheets carry exactly 4, all stated as counts, presences or string tests. But four criteria are currently false as written and are covered by revision requests below: `identity/04` c2, `identity/01` c1, `fantasy/02` c2(b), and `vocabulary/03` c3 (self-declared, 1 known failure).
- **No sheet specs priority-3 content — PASS.** `setting/05` maps all eight priority-3 items to `A17`–`A23` and states "the intended output is a place that cannot host one". `tone/04` D7/D9/D10/D15 name each in order to forbid it, which the pipeline declares compliant. No sheet reserves space for or stubs one.
- **No sheet names a capability absent from the Registry — FAIL, narrowly.** `tone/02`'s delivery requirement states flatly "**UI/UX:** the `collection-index` panel renders one flavour line per Find". Neither registry pattern has a per-item body-text field — `modal-grid`'s item card is `name`, `badge`, `price`, `art`, `artPrompt`, as `lore/_lead.md` records from `ui-forge/src/ideate/brief.mjs`. `tone/02` uses the correct conditional wording elsewhere ("if any ever exists") and carries `deliveryStatus: unbuilt`; this one bullet is imperative and needs the same hedge.
- **Every `[research: url]` is a real fetched source — FAIL.** `vocabulary/02-banned-words.md` and `vocabulary/_lead.md` cite `roblox.com/games/129774084106862/Scrap-Incremental`; `tone/_lead.md`, `tone/01`, `tone/04` and the brief's own `research/landscape.md` cite `roblox.com/games/92876036717311/Scrap-Incremental`. Two place ids, one game name, contradictory quoted descriptions ("Roll rare Relics to power up your journey!" versus "A relaxing magnet simulator game"), both banked into `cid/_research/pack.md` as separate sources. One of the two is wrong or they are two different games, and the category's most expensive ruling (`relic` banned, forcing a rename of `collection.className`) rests on the unreconciled one. Everything else spot-checked resolves and is cross-cited consistently.
- **Every `[cid: decided]` is flagged upward — PASS.** All 20 leaf sheets carry a `## Flagged to the developer` section with live alternatives and the cost of overruling. `vocabulary/02` is the one exception: it carries two `[cid: decided]` tags in `## Consequences` with no flag section, but both are technical corrections (a regex engine mismatch, adding `/`) rather than design choices.

## Revision requests

### `cid/theme/tone/04-do-nots.md` — the exclusion list was renumbered X→D and at least four cited rules disappeared with the old ids
**Violates:** check 2 (category coherence) and the standing rule that a rename must not falsify a downstream citation.
**Fix:** `tone/04` now ships `D1`–`D15`. At least nineteen sheets across five categories cite it as `X`. Four cited rules have no `D` equivalent at all, and two sheets attribute *different* content to the same missing `X10`: `analytics/_category.md`, `analytics/kpis/_lead.md`, `analytics/events/02`, `analytics/funnels/04`, `analytics/engagement/01` and `analytics/economy/03` all read `X10` as *"Measure freely, display none of it"* — declared **binding on all five Analytics domains** — while `setting/03` reads `X10` as forbidding "any surface that displays time". `X1` (*"Nothing moves or sounds that the player did not cause"*), `X2` (the hour), `X9` (the oversized tool) and `X12` (truth-in-depiction reaching store key art) are likewise cited and absent; `D`'s `notAudited` list now *excludes* the store listing and thumbnail, which reverses `X12`'s scope. Restore the four dropped rules as new `D` rows with their original substance, and add an `xToD` mapping table to the sheet so every existing citation resolves. Do not renumber again.

### `cid/theme/setting/03-physical-law.md` — nine citations into Tone name ids that do not exist
**Violates:** check 2; the "cite the string, not the label" standing rule.
**Fix:** `R5`'s check column reads *"this is `tone/04-do-nots` `X1`'s trigger count"* — already filed as a revision request by `cid/audio/ambient/02-the-ambience-word-check.md` `RR3` and still unfixed. Also dangling: `tone/04` `X2`, `X7`, `X10`, `X12`, `XW`; and `tone/03-beat-map` `B3`, `B5`, `B6` are cited with content `tone/03` does not carry (`B5` is *"one patch clears and pays"*, not "elapsed session time may not reach an ambient channel"; there is no `B6`). Replace `R5`'s check with the self-contained count `audio/ambient/02` already drafted, and re-resolve the other eight against the shipped `D`/`B` ids after `tone/04` is fixed.

### `cid/theme/fantasy/02-promise-over-time.md` — cites `tone/03` `M5`, `M10`, `M11`, and a criterion that asserts something else
**Violates:** check 2; criterion truth.
**Fix:** `tone/03-beat-map.md` ships `B1`–`B5`; there is no `M5`, `M10` or `M11`. The re-entry ruling attributed to `M10` and the 24-of-24 ruling attributed to `M11` are not in that sheet under any id. Separately, criterion 2(b) states "`cid/theme/tone/03-beat-map.md` criterion 4 already asserts the events half" — `tone/03` criterion 4 is *"No cue increases in intensity as an area approaches completion, and `B5` has exactly one intensity level"*, which asserts nothing about events conditional on 24 of 24. Re-point the three ids at `B2` (whose row does carry "24 of 24 is the fourth `B2` and nothing further"), and either cite the `B2` row directly in criterion 2(b) or drop the clause.

### `cid/theme/fantasy/03-inhabiting.md` — cites `P10`, `M14`, `X9`, `X10`, none of which exists
**Violates:** check 2.
**Fix:** "register work's `P10`, ten mood words including `relaxing`, `calm`, `cozy` and `peaceful`" is `tone/01`'s **P7**, and its list M holds 29 words, not ten — both the id and the count are wrong, and `Flagged to the developer` repeats `P10`. `M14` (anticipation) is not in `tone/03`. `tone/04` `X10` is quoted in `measuredNotDisplayed.ruledBy` and `X9` in the SKU flag; neither exists. Note `analytics/_category.md` relays `X10` **through this sheet** ("relayed by `theme/fantasy/03`"), so this file is the propagation point for the Analytics-wide claim. Re-point to `P7` with the correct count, and re-point `X9`/`X10`/`M14` after `tone/04` is fixed.

### `cid/theme/setting/04-permanence-and-passage.md` — leans on `tone/03` `K3`, `K4` and `M8`, which are not in that sheet
**Violates:** check 2.
**Fix:** `W1`'s central argument is that "every form of it is already closed", citing `K3` (*"A peak's world footprint is exactly the thing that just changed … no area-wide light, colour, growth or bloom change fires at any peak"*) and `K4`. `tone/03` carries no `K` ids and no rule of that substance; the nearest is the `forbiddenPeaks` row `areaEntryOrExit`, which is what `M8` appears to mean. Re-point all three, or, if the substance genuinely no longer exists in `tone/03`, mark `W1` as resting on `A4`/`A7`/`A10`/`A13`/`R1` alone and say so.

### `cid/theme/vocabulary/01-naming-form.md` — the "Tone's amendment: honoured in full" table maps to Tone rule ids and numbers that no longer exist
**Violates:** check 2.
**Fix:** The table credits Tone with "P6" for casing (now `P9`), "P5" for the character class (now `P4`), and "P4" for the ≤8-word rule (`tone/01` `P3` sets **12** words, not 8, and attributes the tighter per-field ceiling to Vocabulary). Line 60 states Tone's copy rule as "FK **4** target, grade 6 ceiling, no word over three syllables, ≤ 8 words" — `tone/01` P5 sets **5.0** target / 6.0 ceiling and states neither a syllable rule nor an 8-word rule anywhere. The 8-word blurb ceiling in `wordsByPath` is compatible (it is tighter, and `tone/01` P3 defers to the tighter one) but its stated provenance is wrong. Re-point the four ids and restate the two numbers from `tone/01` as shipped.

### `cid/theme/identity/02-role-legibility.md` — permits second person and blesses the exact string Tone requires be rewritten
**Violates:** check 2.
**Fix:** `requiresOfVocabulary.secondPersonPermitted` is `true` and the consequence bullet reads "Second person is fine; *Clear a wider sweep as you walk* passes". `tone/01` P1 bans first and second person over every player-facing string, requests `you`/`your` into `vocabulary.bannedWords`, and names `upgrades[1].blurb` as the single string that breaks on merge with the replacement `"Clear a wider sweep while walking"`. Identity owns grammatical **person for the role**, Tone owns the register; the two cannot both hold. Set `secondPersonPermitted` to `false`, replace the blessed example with `tone/01`'s replacement string, and keep the first-person rule as the additional constraint it was written to be. If Identity intends to overrule Tone instead, that needs a `## Pushing back` naming `tone/01` P1.

### `cid/theme/tone/01-register.md` — P8 permits `ancient`, which Lore forbids and has requested into the merged ban list
**Violates:** check 2.
**Fix:** P8 reads "`old` and `ancient` are permitted, at most one age adjective a string". `lore/01-the-past.md` L3 forbids `ancient` and its `vocabulary` amendment requests it as a hard ban ("dates the place; the only permitted measure of age is quantity of overgrowth"). Lore holds the subject by assignment (era and dating are `canon`'s), so Tone should yield: drop `ancient` from P8's permitted pair and keep `old`. In the same block, `disjointFrom` claims disjointness from "`lore/01-the-past.md` L1/L3/L5" while the P8 group duplicates lore's `lost`, `forgotten`, `cursed`, `haunted` — either remove the four from P8's request or amend the claim to name the overlap and say which sheet's reason string wins.

### `cid/theme/vocabulary/03-term-register.md` — `sure work` is coined by Fantasy and is not in the canonical list
**Violates:** check 1, which is the category's headline check.
**Fix:** Add `{ "word": "sure work", "kind": "writers-handle", "renderable": false, "coinedBy": "cid/theme/fantasy/03-inhabiting.md" }` to `internalTerms`, and move criterion 1's counts from 28/26+2 to 29/26+3. Note this collides with `vocabulary/04`'s `authoredInternalTermsMax: 2` and its check `authoredInternalTermsCount` ("more than 2 authored `internalTerms` entries is a merge problem"), so the cap must move to 3 in the same round or the register cannot legally hold the term it is required to hold. Also note `sure work` contains a space and so fails `04`'s non-renderable regex `^[a-z][a-z0-9-]*$`; decide between `sure-work` as the stored key and a stated exemption.

### `cid/theme/identity/04-no-cast-declaration.md` — criterion 2 cannot pass, and is falsified by sheets that name the words in order to forbid them
**Violates:** the standing rule that a check must be able to pass.
**Fix:** Criterion 2 requires zero whole-word hits for `npc … mascot, guardian, spirit, ghost` inside any ` ```manifest ` block under `cid/`. `tone/04-do-nots.md`'s manifest carries `"a mascot"` in `D13.forbids`; `tone/01-register.md`'s manifest carries `spirit` and `ghost` in the P8 request; `art/characters/01-the-unmodified-body.md:110` carries the full nine-class list — which this sheet authored. Scope the criterion the way `setting/01` and `setting/05` already scope theirs: exempt declared exclusion lists by naming them, e.g. "…excluding `characterArt.zeros.entityClass`, `tonalExclusions.exclusions[].forbids`, and any `bannedWords`-shaped array, which are the check rather than a violation of it."

### `cid/theme/identity/01-player-role.md` — criterion 1's "Verified at 0 as written" is false
**Violates:** criterion truth.
**Fix:** The eleven base words are required to appear zero times inside any manifest block under `cid/`. `guardian` appears in `art/characters/01-the-unmodified-body.md`'s `zeros.entityClass`; `chosen` appears in `analytics/kpis/02` and `analytics/economy/01`; `owner` appears as a field name in a dozen manifests. Apply the same exclusion-list exemption as above and re-state the verified count, or narrow the scope to *player-facing string values* rather than all manifest values.

### `cid/theme/fantasy/03-inhabiting.md` — `assurances.scope` carries another domain's pacing numbers with no re-derivation rule
**Violates:** check 4.
**Fix:** `clearsPerLapAtDepth1: 140`, `findsPerLap: 3`, `areaCompletionsPerLap: 1`, `lapsPerBoundSession: [3,7]` and `fractionOfClearsCarryingNothingAboveATick.floor: 0.95` are values owned by `gameplay/core-loop` and derived from `collection.relicsPerArea`. Replace the literals with a `source` naming the owning key and field plus a `reDerivation` expression (e.g. `fractionOfClearsCarryingNothingAboveATick = 1 - (relicsPerArea + 1) / clearsPerLap`), the way `setting/04`'s `openSeam` and `identity/03`'s `doNotCopyTheNumber` handle the same problem. The guarantee `C1`–`C6` content is untouched by this; only the scope block moves.

### `cid/theme/vocabulary/02-banned-words.md` — the `relic` ban cites a Scrap Incremental place id that contradicts the brief's and Tone's
**Violates:** the `[research: url]` invariant.
**Fix:** This sheet cites `https://www.roblox.com/games/129774084106862/Scrap-Incremental` for *"Roll rare Relics to power up your journey!"* and the *"Tier system"*. `research/landscape.md` (the brief's own fetch), `tone/_lead.md`, `tone/01` and `tone/04` all cite `https://www.roblox.com/games/92876036717311/Scrap-Incremental` and quote *"A relaxing magnet simulator game"*. Both ids are banked in `cid/_research/pack.md` as if they were distinct sources. Reconcile: either name the two games distinctly (the 129… entry may be a different title) or correct the id. This matters because the `relic` ban forces a rename of `collection.className`, which is the most expensive consequence anything in this category produces, and it currently rests on a source another sheet in the same category identifies as a different game. The `Faith Incremental` citation is independent and unaffected.

### `cid/theme/vocabulary/_lead.md` — the index states Theme owns zero contract keys, which is now false
**Violates:** the domain-index contract rule ("a domain does not need a contract key to run, it needs to produce one").
**Fix:** Lines 23 and 37 read "Nine keys" and "**Vocabulary owns zero contract keys. So does every other domain in Theme & Narrative.**", and gap G4 repeats it. The contract is 26 keys and `vocabulary` is owned by `theme/vocabulary` — `vocabulary/02-banned-words.md` provides it and the merger enforces it on every player-facing string. Every other lead in this category has already corrected the same line and said so on the record; this one has not. Update the key table and rewrite G4 as closed. `vocabulary/04-coinage-intake.md` carries the same stale figure ("`SCHEMA` holds **11 keys** across 8 owning sheets") and should be corrected in the same round.

### `cid/theme/tone/02-flavour-and-humor.md` — the delivery requirement names a render capability the pattern registry does not have
**Violates:** the Build Capability Registry invariant.
**Fix:** `## The delivery requirement` bullet 2 states "**UI/UX:** the `collection-index` panel renders one flavour line per Find, visible only after that Find is revealed". `modal-grid`'s item card has no body-text field. Reword to the conditional form this sheet already uses in its containment table: "*if* a per-item body-text field is added to the registry, the `collection-index` panel renders…", and add the pattern-registry gap to `flavourText.deliveryStatus`, which currently records only the missing `{name, flavour}` data shape and not the missing render capability.

## Predicted cross-category conflicts

Recorded for the final pass; none is a Theme failure today.

1. **The `X10` blast radius.** Analytics declares *"Measure freely, display none of it"* binding on all five of its domains (`analytics/_category.md`, `kpis/_lead.md`, `events/_lead.md`, `funnels/_lead.md`, `economy/_lead.md`, `engagement/_lead.md`) and sources it to a Tone rule that no longer exists. If `tone/04` restores it under a `D` id, ~12 Analytics citations need re-pointing; if it does not, Analytics has an unsourced binding constraint and `kpis` `K7`, `funnels` `X8` and `events` `N12` lose their only justification. This is the largest single downstream item in the repo right now.
2. **`ancient` versus the Art prompts.** If lore's 25 bans merge, `ancient` becomes a hard merge failure on any player-facing string. `01-FOUNDATION.md`'s own phrase is *"ancient stone"* and Art's environment prompts are the likeliest holders. Lore scopes `canon` to `artPrompt` deliberately; `vocabulary.bannedWords` does not reach `artPrompt`, so the two halves will diverge unless the `RW`/`canon` lint is built.
3. **`upgrades[1].blurb`.** Whatever Theme settles, `gameplay/balance` owns the string. `tone/01` supplies the replacement; `identity/02` blesses the original. Balance will act on whichever it reads last.
4. **`sure work` and the coinage cap.** `vocabulary/04` caps authored `internalTerms` at 2 "ever" and makes a third a merge problem. Waves 4–7 (UI/UX, Audio, Analytics, Live Ops, Marketing) all own zero contract keys and will need this route; the cap plus the missing ` ```coinage ` parser means their coinages have nowhere legal to go.
5. **Six proposed keys, none promoted.** `setting`, `canon`, `assurances`, `flavourText`, `peakPolicy`, `tonalExclusions` are all `status: proposed`. Downstream sheets already cite them as if merged (`art/environment` cites `setting.contents` ids by hand; `art/vfx` carries `D2`/`D5` as rows). Until the schema owner promotes them, every one of those is a hand-copy that can drift.
6. **`terrace` spelled two ways.** `vocabulary/03` records the one in-manifest one-spelling failure (`TERRACE` in `area.label`, `Terrace` in `collection.sets[0].label`). It is `gameplay/meta`'s to fix, and three Theme sheets assert it will be fixed.

## On the three specific claims put to me

- **`allowedPattern` divergence — the claim is stale and the sheet is already correct.** `tone/01` P4 as it now stands lists `/` among the six permitted marks and does not ban it; the divergence was real and has been closed. `vocabulary/02` is the authority on the `%%` and `/` changes and `tone/01` restates it accurately. No action.
- **`upgrades[1].blurb` violates P1 — confirmed.** The string is `"Clear a wider sweep as you walk"` (`identity/02`'s `shipped.blurbs`, corroborated by `vocabulary/01`'s criterion 4). P1 bans `you`. `tone/01` declares it in `breaksOnMerge` with a replacement, which is the correct handling. The live problem is not Tone's — it is that `identity/02` blesses the string. See the revision request.
- **CLAUDE.md's description of Tone — the correction is directionally right and slightly overstated in the other direction.** CLAUDE.md says four sheets of register prose became `casing`, `maxSentenceWords` and `allowedPattern`; `bridge/schema.mjs:142` says "Tone spent four sheets deciding a register. One ruling from it, Title Case, had to be carried into two other domains by hand." Tone's claim that only sheet `01`'s P9/P3/P4 reached the build is closer to the truth than CLAUDE.md's. But `tone/_lead` then claims those three values as sheet `01`'s, and `vocabulary/02`'s own record says `allowedPattern`'s shape was found by the hud-binding builder and its `/` added by Vocabulary on 2026-08-01. Two of the three are Tone's; the third is jointly owned. This meets neither stopping-rule bar and I am **not** raising a revision request — but if CLAUDE.md is edited on Tone's say-so, the line should read "one Tone sheet's three rules, plus two fixes by the key's owner."

## Noted and deliberately not acted on

Meets neither bar: a player would not notice and two builders would not diverge.

- `tone/01`'s path table names `collection.sets[].relics[].name` as a live path; relics currently ship as bare strings, so the real path is `collection.sets[i].relics[j]`. `playerFacingStrings()` handles both.
- `tone/01` counts eleven player-facing path classes, `vocabulary/01` and `03` count ten; the difference is whether prose paths are included. Both are right about their own scope.
- `vocabulary/03` cites `tone/04-do-nots.md` as one of four files recording `Heartvine` as passing; `tone/04` does not mention `Heartvine`. Harmless.
- `vocabulary/04` says "`tone/04` recorded that *pass 1 has no home*"; no such line is in `tone/04`. Harmless.
- `identity/04` line 43 self-reports its own index as stale at "ten keys"/"eleven"; the real figure is 26. It flags the staleness, which is the right behaviour.
- `vocabulary/02` carries two `[cid: decided]` tags with no `## Flagged to the developer` section. Both are engine-level corrections, not design choices.
- `setting/03` honestly reports that the category's anti-cyclical gate does not actually forbid a day/night cycle and rules against one on four independent grounds. That is a finding about `_category.md`, correctly flagged upward, not a defect in the sheet. **Recommend the category lead narrow the gate's wording** so a later sheet does not lean on it for more than it carries.

## What must happen before this category can release

1. `tone/04` restores the four dropped exclusions and publishes an X→D mapping. Nothing else in the list of fifteen can be resolved before this, because five sheets are waiting to re-point at it.
2. The five Theme sheets with dangling Tone citations re-point: `setting/03`, `setting/04`, `fantasy/02`, `fantasy/03`, `vocabulary/01`.
3. The two live contradictions are settled by one sheet yielding, on the record: `identity/02` versus `tone/01` on second person, and `tone/01` versus `lore/01` on `ancient`.
4. `vocabulary/03` collects `sure work`, and `vocabulary/04` raises its cap to admit it.
5. `identity/04` c2 and `identity/01` c1 are rescoped so they can pass.
6. `fantasy/03`'s `assurances.scope` replaces copied pacing numbers with a source and a re-derivation.
7. `vocabulary/02` reconciles the Scrap Incremental citation, because the `relic` ban rides on it.
8. `vocabulary/_lead` and `vocabulary/04` correct the "Theme owns zero keys" / "eleven keys" figures.
9. `tone/02`'s delivery bullet becomes conditional.
10. Re-run `npm run cid:verify` and `npm run bridge`, then re-verify. **On a re-run I will re-read every changed file; nothing above is approved by prior verdict.**

**Out of scope for this category but must be raised to whoever sequences CID:** the `X10` citation failure reaches Analytics' category brief and four of its domain leads, `liveops`, and `audio/ambient`. Those are not mine to revise, and `analytics/_verified.md` and `liveops/_verified.md` were both written against an id that no longer resolves.
