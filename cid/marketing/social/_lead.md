# Social — domain index

**Category:** Discovery & Marketing · **Wave:** 7 · Reads: `HANDOFF.md`, `CONCEPT.md` (via
`HANDOFF`'s routing rule), `00-CORE.md`, `03-META.md`, `05-OUTWARD.md`, `OPEN.md` §1–§5,
`cid/marketing/_category.md`, `cid/liveops/_category.md`, `cid/_contract.md`, `cid/_state.md`,
`cid/gameplay/monetization/02-what-is-never-sold.md`, `cid/theme/tone/02-flavour-and-humor.md`,
`cid/audio/music/01-whether-music-exists.md` (as the house form), `bridge/schema.mjs`,
`bridge/context.mjs`, `game/src/`, `ui-forge/src/`, `ui-forge/briefs/`.

**Conclusion, stated first because it is the output:** this game maintains **no presence outside
the Roblox experience page**, and the strongest thing closing it is not scope. It is that a Roblox
social media link is, since 2026-06-30, viewable and manageable only by **age-checked 16+ users**,
against an audience the brief binds at **8–14**. A channel this project opened could not be seen by
a single member of its own stated audience.

---

## What the brief gave me

| quote | provenance |
|---|---|
| *"Success is **shipped artifacts, not players**."* `00-CORE.md` | `[brief: binding]` ← `[you chose: R1 Q3]` |
| *"**Beating the genre's retention curve.** Offered and declined."* · *"**Revenue.** Offered and declined."* `00-CORE.md` | `[brief: binding]` ← `[you chose: R1 Q3]` |
| *"content design is the primary creative work on this project, not art or **marketing**."* `00-CORE.md` | `[brief: binding]` ← `[you chose: R1 Q1]` |
| *"the **smallest game** that still gives every creative area real work."* `00-CORE.md` | `[brief: binding]` ← `[you chose: R1 Q3]` |
| *"**8–14**, mobile-heavy, short sessions."* `00-CORE.md` | `[brief: binding]` ← `[you chose: R1 Q4]` |
| *"**Ships and settles. No seasons or events.**"* `OPEN.md §2` | `[brief: soft]` ← `[I assumed — batched]`, **0 interview questions** (`OPEN.md §1`) |
| priority 3: *"codes · daily rewards · leaderboards · trading · seasons and events"* `03-META.md` | `[brief: soft]` ← `[I assumed — the ordering]`, hard as a gate |
| *"Left open — the name, icon, thumbnail, and store description."* `05-OUTWARD.md` / `OPEN.md §4` | `[brief: soft]` — **four artifacts, none of them a channel** |

**Approved sheets that reach this subject, cited not restated:** `products` `F15` (no code field
and no like, favourite, follow, group-join, rate-us or share prompt **anywhere in the game**;
`F19`, no product named anywhere inside the game); `theme/tone/02` (the humor ban, `[cid: decided]`
extension, names *"group posts"* as a forbidden surface); `notices` (two members, both beats);
`social.chat` off on all three surfaces; `release.shutdown.playerFacing: "nothing"`;
`endgame.forbidden` (`redeemCode`, `dailyReward`, `seasonPass`, `seasonalEvent`, `leaderboard`,
`trading`); ruling **R-4** (`products.storeExists: false`).

**The one line that argues against me, relayed rather than omitted:** *"Codes for free boosts, and
a group-join reward. Both are near-universal on Roblox incrementals and **their absence reads as an
unfinished game**."* `[research: research/grass-incremental.md]`. `HANDOFF.md` says argue with the
source. The sheet must, and the argument available is that "reads as unfinished" is an unattributed
impression about **retention**, which is a declined goal, while the 16+ link rule is a platform
constraint that does not care what the genre expects.

## What the brief did not give me

| gap | routed to |
|---|---|
| **G1 — the brief states no off-Roblox presence anywhere.** No group, no Discord, no account, in any of five layer sheets, `OPEN.md` or `research/`. Live Ops flagged the identical gap from the intake side; its category document records the referent as *"absent — not forbidden, not stated"* | **the presence half** is sheet `01` here. **The intake and moderation half is not mine**: it is community-management and feedback-triage work *[currently Live Ops — Community]*. Neither of us can close G1 alone and neither should pretend to |
| **The brief never says who would operate a channel.** The project has one developer; `cid/_playtest.md` is `n = 1` | sheet `01`, as a costed term in the reversal path, not as a staffing plan |
| **The brief gives an 8–14 band and no under-13 share.** The platform's own bands are Under 9 / 9-12 / 13-15 / 16-17 `[research: https://about.roblox.com/newsroom/2025/11/roblox-requires-age-checks-limits-minor-and-adult-chat]`; `theme/tone/01` cites 35% of age-checked daily users under 13 **platform-wide**, not for this game | sheet `01`, which must state that the ruling **does not depend on the split** — every band below 16 fails the link test identically |
| **Nothing in the brief, either contract, or `docs/cid-workflow.json` makes an outward channel a build artifact.** `channels` would merge and reach no emitter — `M6` in the category document, restated for this key | contract-and-seam work; sheet `01` states its own emitter hole rather than assuming one |
| **`theme/tone/02` forbids humor in *"group posts"* — a surface the brief never established.** A zero here makes that row vacuous | sheet `01` records the row as vacuous and does **not** edit it; whether the row is rewritten is register work *[currently Theme — Tone/Vocabulary]* |
| **Whether the *absence* of a community or Discord is ever stated outward.** `CONCEPT.md` argues for saying cut things plainly in-project; whether that reaches a listing is not mine | store-listing copy work *[currently Marketing — Store Page]*, as a consequence |

## Why 1 sheet

**One contract key, one sheet.** I own — propose — exactly one key, `channels`, and every part of my
`owns` list resolves to one row inside it. There is no second decision here: the group, Discord, the
three video/short-form accounts, the cadence, the pillars and the outreach are not five judgements
that happen to agree, they are **one ruling with five referents**, and the evidence closing them is
shared (a binding non-goal, a platform age rule, and `F15`). Splitting would produce the wave-1
failure with a smaller subject: four sheets saying nothing four times. The category document
pre-empted it in one line — *"One sheet. Do not commission a second."* I also considered and
rejected a second, non-value sheet on *the reopening condition*: it constrains `channels` and only
`channels`, so it belongs inside the key as a field, not beside it as prose.

## The subject, enumerated: five parts, five closures, one extra surface

Stated here so a verifier reads a conclusion. The writer receives them through the row below.

| # | my `owns` part | ruling | what closes it, strongest first | observable |
|---|---|---|---|---|
| 1 | **the Roblox group** | **none created.** `robloxCommunityCount: 0` | **Not** closed by the link rule — a community is on-platform. Closed by `products` `F15` (no group-join prompt anywhere in the game) plus its grant half (no group-join, follow, like, favourite or share reward), so **every path to one is removed even though its existence is not forbidden** — and by `00-CORE.md`'s binding non-goals, which leave nothing for it to do. **This is my weakest closure and the sheet must say so.** | count of Roblox community ids named in any `cid/**` manifest or emitted config: 0; count of player-facing string literals matching `/group\|community\|join us/i`: 0 |
| 2 | **Discord** | **no server.** `discord.exists: false` | **Compliance, not scope.** Discord is one of the seven linkable types, and social media links are viewable, shareable and manageable only by age-checked **16+** users since 2026-06-30 — so no member of an 8–14 audience can see the link. Discord's own terms require **13+**, excluding roughly the lower half of the band outright. Roblox additionally prohibits creators posting social media links **inside experiences**. Then `F15`, then the unfunded moderation obligation | count of Discord invite urls anywhere in `cid/**` or `game/src`: 0; count of experience-page social links of type Discord: 0 |
| 3 | **TikTok / YouTube / X presence** | **no accounts.** `channelCount: 0`, `channels: []` | Same 16+ rule makes any link unshowable to the audience; TikTok requires **13+** (14+ in Florida) and YouTube **13+**, so the lower half of the band cannot hold an account; and `00-CORE.md`'s *"shipped artifacts, not players"* removes the purpose | `channels.channelCount == 0` and equals `len(channels.channels)`; count of external urls in any of the six marketing keys: 0 |
| 4 | **posting cadence and content pillars** | **vacuous, not merely empty.** `postingCadence: "none"`, `contentPillars: []` | There is no channel to post on (rows 1–3); *"ships and settles"* leaves nothing to announce; `endgame` is unbounded and unchanging; `notices` holds two beats and cannot carry a non-beat; Live Ops' Events and Seasons are expected to rule zero. **The sheet must state the vacuity in one line** rather than leave a verifier to guess, the way Events was told to for unspent event currency | `channels.contentPillars == []`; count of scheduled outbound artifacts in `roadmap`, `events` or `launchBeats` naming a channel: 0 |
| 5 | **creator and influencer outreach** | **none.** `outreach.contactCount: 0`, `outreach.paidPlacements: 0` | *"Beating the genre's retention curve. Offered and declined"* and *"Revenue. Offered and declined"* `[brief: binding]` ×2 close it by the brief's own words; and **there is nothing to send** — no capture is legitimate from any build that exists (`M2`/`G2`: the shipped theme is `cartoon-vibrant` / `"Pet Ascend Simulator"`) | count of outreach contacts, briefs, keys or codes issued: 0; count of marketing `claims[]` rows addressed to anyone but a player: 0 |
| + | **the experience page's own social-link fields** (category row 12, assigned to me) | **zero of the three permitted slots filled** | The platform allows **up to three** links across **seven** types; adding one requires the creator to be age-checked **16+** by facial age estimation or government ID, and viewing requires the same. A field that no member of the audience can read is not a discovery surface | `experiencePageSocialLinks.count == 0` against `maxPermitted: 3`; Creator Hub → Creations → Social Links holds zero entries |

**The invariant that makes this a build instruction rather than a note:** `channels` is the **closed
enumeration of every channel this project has**, and it is empty — so any channel, invite, handle,
group id or external url named by `title`, `storeIcon`, `storeThumbnails`, `storeListing` or
`launchBeats` fails against it. A builder or an operator cannot open one without a key failing.

## The `channels` key — the shape my subject needs

**`channels` does not exist in `bridge/schema.mjs`** (grepped this run: no match) and is not among
the 25 in `cid/_contract.md`, nor on the category document's claimed-names list. It is a proposal.
`social` is taken by `gameplay/social/01` and is not requested.

What it must hold: `channelCount` and `channels[]`; `robloxCommunityCount`;
`experiencePageSocialLinks` with `count`, `maxPermitted`, `platformTypeCount`,
`viewableByAgeCheckedMin`, `addableByAgeCheckedMin`; `postingCadence` and `contentPillars[]`;
`outreach`; `externalUrlCount`; a `forbidden[]` of rows carrying `id`, `form`, `closedBy`,
`observable`; a `platformRules[]` carrying the fetched age constraints with their urls; and a
`reopening` object with two independent gates. **Zero `null` tokens** — `tech/deploy/02` makes an
emitted null a hard error and `music/01` criterion 1 is the precedent. Useful cross-key invariant
for whoever writes the shape: `channelCount == len(channels) == 0` **and** `externalUrlCount == 0`
summed across all six Discovery & Marketing keys.

| # | sheet | must decide |
|---|---|---|
| 01 | `no-off-platform-presence` | Rule this game's entire off-platform presence to zero and supply the proposed `channels` key as an explicit empty set with a row each for the Roblox community, Discord, TikTok, YouTube, X, posting cadence, content pillars, creator and influencer outreach and the experience page's three social-link slots, every row carrying the approved sheet or platform rule that closes it and an observable that is a count; lead on the compliance closure rather than the scope one, because since 2026-06-30 a Roblox social media link is viewable, shareable and manageable only by age-checked 16+ users while this game's audience is bound at 8–14, Roblox separately prohibits creators posting social media links inside experiences, and Discord, TikTok and YouTube each set a 13+ floor beneath the lower half of that band; state plainly that the Roblox community is the one row the age rule does **not** reach, so it rests on `products` `F15` and the binding non-goals alone; record the cadence and pillar rows as vacuous rather than unfilled; state the reopening condition as two independent gates, a developer ruling against `00-CORE.md`'s binding *"shipped artifacts, not players"* **and** a 16+ facial-age or government-ID check on the operator after which the audience still cannot see the link, and price the reversal in those terms; and make the key the closed enumeration of channels so that any channel, invite, handle, group id or external url named by any other marketing key fails against it. |

## Verification note

**This sheet is most likely to be contradicted by community-management and feedback-intake work**
*[currently Live Ops — Community]*, which runs in the same wave and holds the other half of G1. If
it concludes that a feedback channel must exist, the cheapest form of one is a Discord, and it will
land on a key that says there is none. **The seam I am asserting, so it can be checked rather than
discovered:** I rule on whether an **outbound** channel exists; Community rules on how **inbound**
feedback is received and moderated, and `cid/_playtest.md` is already an inbound artifact that needs
no channel. If Community needs an off-platform intake, that is a revision request against this key
naming the row, and it inherits the same 16+ evidence — which is the point of ruling on compliance
rather than on scope: the scope closure can be reversed by a developer in one line, and the
compliance closure cannot.

Second most likely: **contract-and-seam work**, if `channels` is judged to duplicate `community` or
to be unmergeable because no emitter reads it. Third: **Hype**, whose launch beats must not assume a
posting surface, and **Store Page**, whose listing must contain no external url.

**One correction to a claim two category documents make, verified this run rather than inherited.**
Both call `F15` *"grep-checked"*. The `TextBox` half is clean and I confirmed it: **zero**
`Instance.new("TextBox")` anywhere in the repo, zero `TextBox` nodes in `ui-forge/briefs/`, and the
only `TextBox` mentions in `game/src` are defensive — a type union at `game/src/client/HudBinding.luau:86`,
two `IsA("TextBox")` branches at `:241` and `:258`, and `GetFocusedTextBox()` at
`game/src/client/Input.luau:272` under a comment reading *"Nothing in this game creates a TextBox"*.
**The `/group/i` half is not clean as a raw repo grep** and returns at least five false positives —
`PROGRESS_GROUP = "ProgressGroup"` (`HudBinding.luau:104`), `colorToken(group, …)`
(`Pressables.luau:208–211`), `groupGap` and `Group_` (`IndexScreen.luau:323–363`), and collision
groups in `init.server.luau` and `Plots.luau`. `F15`'s own row scopes itself to *player-facing
strings*, so the row is correct and the **shorthand** is what misleads. Anyone running that check
must scope it to player-facing string literals. **This is a floor, not a total: I ran one pattern
over `game/src` and did not prove an upper bound.**

**And one capability claim I checked before resting on it.** The code-redemption closure rests on
`F15` and on priority 3, **not** on a toolchain incapability: `ui-forge` can build a `TextBox`
(`ui-forge/src/emit/runtime/UIBuilder.luau:390`, `ui-forge/src/transpile/to-html.mjs:309–324`). No
brief uses one. Saying "it cannot be built" would have been false and would have collapsed the
moment someone opened the file.

## Research owed

**My graph node carries no `must_verify`** (`docs/cid-workflow.json`, `social-lead`, verified this
run — the node has `owns`, `inputs`, `outputs`, `writes_to`, `description`, `does_not_own` and
nothing else). The three checks below were assigned by my category lead, and I treated the age
question as the one most likely to convert a scope closure into a compliance one. It did.

**Fetched and banked:**

- Social media links on an experience page or community: *"you can add up to three links to social media sites on your game details pages"*, across *"seven different social media types … Facebook, Twitter, YouTube, Twitch, **Discord**, Guilded, and a Roblox community"*, and *"Social media links are only visible to users who have verified their age as at least 16 years old"*, with adding requiring the same 16+ check by facial age estimation or government ID. `[research: https://github.com/Roblox/creator-docs/blob/main/content/en-us/production/promotion/social-media-links.md]`
- The 16+ threshold's effective date and that it was **raised** from 13+: *"Starting June 30, social media links on Roblox will only be viewable, shareable, and manageable by age-checked users 16 and older, raised from the current requirement of 13+"*, applying across profiles, game detail pages, Community pages and Creator Hub. Today is past it. `[research: https://devforum.roblox.com/t/proposal-parental-consent-for-13-15-users-to-view-social-media-links-on-roblox/4686207]`
- The underlying obligation, quoted in the same forum from the April 2026 Alabama settlement: *"By default, Roblox shall not allow U16 Users to see or share profile links to other approved sites. Roblox may permit Users aged 16 or older to share profile links to other approved sites only with other Users of a similar age."* **This is why the closure is durable: it is a settlement term, not a product preference.** `[research: https://devforum.roblox.com/t/allow-users-age-checked-13-15-with-parental-consent-to-see-profile-social-media-links-not-in-regions-where-social-media-is-banned-for-under-16/4693383]`
- Links inside an experience, independent of `F15`: *"Early next year, we will prohibit creators from sharing or posting social media links in experiences"*, with the platform's six age bands given as *"Under 9, 9-12, 13-15, 16-17, 18-20, or 21+"* — the brief's 8–14 straddles three of them and reaches none that can see a link. `[research: https://about.roblox.com/newsroom/2025/11/roblox-requires-age-checks-limits-minor-and-adult-chat]`
- Discord's own floor: *"By accessing our services, you confirm that you're at least 13 years old and meet the minimum age required by the laws in your country"*, and *"Our services are not designed for nor directed towards users under the age of 13"*. `[research: https://discord.com/terms]`
- TikTok's own floor: *"If you are under 13 years of age, or under 14 years of age in Florida, you may not use the Platform"*. `[research: https://www.tiktok.com/legal/page/us/terms-of-service/en]`
- YouTube's own floor: *"You must be at least 13 years old to use the Service"*. `[research: https://www.youtube.com/t/terms]`

**Could not fetch, and what would settle each:**

- **X's terms of service.** Two attempts, two different hosts, both refused (`https://x.com/en/tos` HTTP 402, `https://help.x.com/en/rules-and-policies/twitter-age-screening` HTTP 403). `[unverified: X's stated minimum account age]` — settled by rendering either page. **Nothing rests on it**: the X row is closed by the 16+ link rule and the binding non-goal, and its own floor would only add a third reason.
- **The Roblox Community Standards page itself** (`https://en.help.roblox.com/hc/en-us/articles/203313410`, HTTP 403; the same domain refused a second article, so this is a host-level block on this tool rather than a bad path). A search summary attributes to it that *"users under 13 [may not] share off-platform links in any way including through the API"*. `[unverified: the Community Standards' own wording on under-13 off-platform links]` — settled by rendering that article, or the same text in the `Roblox/creator-docs` GitHub mirror. **Nothing rests on it**: the fetched 16+ rule is strictly stronger and covers the whole 8–14 band.
- **The cost of creating a Roblox community.** A search summary says 100 Robux, one-time, up to 100 per user; the two sources that would confirm it refused (`en.help.roblox.com/hc/en-us/articles/203313730` and `roblox.fandom.com/wiki/Community`, HTTP 402). `[unverified: the Robux cost of creating a community]` — settled by rendering either. It is a **reversal-cost** figure only; the sheet should carry it as unverified or omit the number and keep the shape of the cost.
- **`npm run bridge -- --contract` was not run**: this agent has no shell tool. Instead I read the derived `cid/_contract.md` (25 keys, regenerated from `bridge/schema.mjs`) and grepped `bridge/schema.mjs` for `channels` directly — no match. Both are checkable by anyone who does have a shell.
- Recorded so nobody re-pays it: `https://create.roblox.com/docs/production/promotion/social-links` is a **404**. The live path is `.../social-media-links`, and the GitHub mirror above is what actually renders.
